version: "3.8"

services:
  microui-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: microui-server
    ports:
      - "8080:8080"
    command: ["microui", "server"]
    environment:
      - name=value
